[user]
	name = John Cornish
	email = cornish@mymail.mines.edu

[alias]
	s = status
  p = !"git add .; git commit -m $1 && git push origin master"
	c = clone
	l = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit

[push]
  default = matching
